package com.example.easyssh.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ──────────────────────────────────────────────
val BgDeep     = Color(0xFF0A0C10)   // główne tło
val Surface    = Color(0xFF111318)   // tło ekranów / kart
val Surface2   = Color(0xFF181B22)   // karty / dolna nawigacja
val BorderClr  = Color(0xFF1F2430)   // obramowania
val TerminalBg = Color(0xFF070A0E)   // okno terminala

// ── Akcenty ──────────────────────────────────────────────────
val AccentGreen  = Color(0xFF00FF88) // główny akcent (SSH / OK)
val AccentBlue   = Color(0xFF00AAFF) // akcent wtórny (Dev / klucze)
val AccentRed    = Color(0xFFFF5F57) // Prod / błąd
val AccentYellow = Color(0xFFFFBD44) // QA / ostrzeżenie

// ── Tekst ────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFE2E8F0)
val TextSecondary = Color(0xFF8892A4)
val TextTertiary  = Color(0xFF4A5568)

// ── Terminal ─────────────────────────────────────────────────
val TermPrompt = Color(0xFF4A7A5A)   // linia prompt $
val TermOutput = Color(0xFF7EC88A)   // wyjście komendy
